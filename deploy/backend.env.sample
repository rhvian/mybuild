# L2 backend 运行时配置（systemd drop-in）
# 拷到 /etc/mybuild/backend.env 后用 sudo systemctl edit 追加
MYBUILD_JWT_SECRET=生产环境请替换为 openssl rand -base64 48 生成的随机串
MYBUILD_BOOTSTRAP_ADMIN_EMAIL=admin@your-domain.com
MYBUILD_BOOTSTRAP_ADMIN_PASSWORD=生产环境请替换
MYBUILD_CORS_ORIGINS=["https://your-domain.com"]
